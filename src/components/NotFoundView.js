import React, { PropTypes } from 'react'

const NotFoundView = React.createClass({
  render () {
    return (
      <div>
        This is a NotFoundView
      </div>
    )
  }
})

export default NotFoundView